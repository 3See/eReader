section.content(data-ng-controller="HomeController")
    .row
        .well(class="col-md-8 col-sm-8 col-xs-8 col-lg-8 col-sm-offset-2 col-xs-offset-2 col-md-offset-2 col-lg-offset-2") 
            button.btn.btn-default.btn-block(ng-repeat="study in studies", ui-sref="study-info") {{study.name}}
    .row
        ul
            li Current Study: {{AppState.current_study}}
